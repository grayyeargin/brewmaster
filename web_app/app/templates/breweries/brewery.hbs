<div class="brewery-page">
	<div class="full-background brewery-background">
		
		<div class="container">
			
			<div class="row">
				<div class="col-12 col-md-6">
					<h1 class="text-large">{{model.name}}</h1>
					<div class="brewery-info">
						<div class="address">{{model.address}}</div>
						<div class="phone">{{model.phone}}</div>
						<div class="rating">{{model.ratingPct}}</div>
					</div>
				</div>

				<div class="col-12 col-md-6">
					
				</div>
			</div>

		</div>

	</div>

	<div class="container">
		{{beers/beer-table beers=model.beers}}
	</div>
</div>

{{outlet}}
